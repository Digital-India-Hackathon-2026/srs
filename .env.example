# OpenAI — used for chatbot (primary)
OPENAI_API_KEY=

# Google Gemini — used as fallback if OpenAI key is missing
GEMINI_API_KEY=

# Optional: change model (default: gpt-4o-mini)
# OPENAI_MODEL=gpt-4o-mini
